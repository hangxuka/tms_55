<div id="trainees" class="tab-pane fade">
  <table class="table">
    <thead>
      <th><%= t "index" %></th>
      <th><%= t "name" %></th>
      <th><%= t "email" %></th>
    </thead>
    <tbody>
      <% @users.each_with_index do |user, index| %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
